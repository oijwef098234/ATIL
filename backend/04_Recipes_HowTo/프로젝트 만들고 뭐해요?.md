1. propotice지우고 yml을 이렇게 세팅한다.

```yaml
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db이름?serverTimezone=Asia/Seoul
    username: root
    password: 비밀번호입력
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
```

1. mysql로 로컬에 프로젝트에 맞는 db를 만든다.
2. 그 다음에 yml과 db를 연결한다.
3. 알잘딱으로 코드 작성 ㄱ